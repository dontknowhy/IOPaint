# NOTE: torch/torchvision must be built for CUDA 12.1 (cu121) and bundle cuDNN 8.9.x,
# because cuDNN >= 9.1 dropped support for SM < 7.5 GPUs (e.g. Pascal P104).
# Install from a CUDA index, e.g.:
#   pip install -r requirements.txt --index-url https://mirror.sjtu.edu.cn/pytorch-wheels/cu121
# (nvidia-cudnn-cu12==8.9.2.26 and other nvidia-* runtimes are pulled in by torch's own pins)
torch==2.3.1+cu121
torchvision==0.18.1+cu121
opencv-python
diffusers==0.34.0
huggingface_hub==0.36.2
accelerate
peft==0.20.0
transformers>=4.41.2,<5
safetensors>=0.8.0
controlnet-aux==0.0.10
fastapi>=0.115.2,<1.0
uvicorn
python-multipart
python-socketio==5.16.4
typer>=0.12,<0.28
pydantic>=2.5.2
rich
loguru
yacs
piexif==1.1.3
omegaconf
easydict
gradio==5.50.0
typer-config==1.4.0

Pillow>=10.0.0 # for AnyText
